customCommands:
  - key: 'C'
    command: "aicommits --type conventional"
    context: 'files'
    subprocess: true
git:
  paging:
    colorArg: always
    pager: delta --dark --paging=never 
promptToReturnFromSubprocess: false
