customCommands:
  - key: c
    description: "AI Commit (aicommit2)"
    context: files
    command: "sh -c 'aicommit2 --confirm'"
    output: terminal
  - key: C
    description: "AI Commit (show message before commit)"
    context: files
    command: "aicommit2"
    output: terminal
git:
  pagers:
    - colorArg: always
      pager: delta --dark --paging=never
promptToReturnFromSubprocess: false
